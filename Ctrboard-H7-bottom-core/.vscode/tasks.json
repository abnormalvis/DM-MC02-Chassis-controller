{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "CMake: Configure+Build Debug",
            "type": "shell",
            "command": "cmake --preset Debug; cmake --build --preset Debug",
            "problemMatcher": [
                "$gcc"
            ],
            "group": "build"
        }
    ]
}
